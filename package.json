{
  "name": "grannys-takeaway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix server && npm run build --prefix frontend",
    "start": "concurrently \"npm run start --prefix server\" \"npm run preview --prefix frontend\""
  },
  "devDependencies": {
    "@types/js-cookie": "^3.0.6",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "framer-motion": "^12.23.22",
    "js-cookie": "^3.0.5",
    "react-icons": "^5.5.0"
  }
}
